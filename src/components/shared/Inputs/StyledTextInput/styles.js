import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    width: 320,
    height: 40,
    backgroundColor: '#fff',
    marginTop: 20,
  },
  text: {
    width: 320,
    fontSize: 14,
    borderRadius: 5,
  },
});
