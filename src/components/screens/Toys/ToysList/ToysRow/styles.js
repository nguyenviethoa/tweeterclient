import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  rowSmall: {
    flexDirection: 'row',
    marginBottom: 4,
    justifyContent: 'space-between',
  },
  rowBig: {
    marginBottom: 4,
  }
});
